import pandas as pd
from scipy import sparse
import numpy as np
from sklearn.neighbors import KNeighborsClassifier
from sklearn.neural_network import MLPClassifier
from sklearn.metrics import accuracy_score, f1_score, classification_report

#Eine Klasse (z.B. TextClassifier) oder Funktionen für:
class Text
#Initialisierung (Modellwahl: KNN, MLP, etc.)
#Training (fit)
#Vorhersage (predict)
#Evaluation (evaluate)
#Optional: Methoden für das Speichern/Laden von Modellen